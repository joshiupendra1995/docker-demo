name : BuildApp

on:
  merge_request:
    branches:
      - main


jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4.1.1
      - name: build app
        run: mvn clean install
